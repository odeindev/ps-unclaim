# AutoUnclaim

Плагин для Minecraft, который автоматически удаляет регионы ProtectionStones у неактивных игроков.

## Возможности

- **Dry-run режим** 🆕 - предпросмотр изменений без удаления
- **Гибкая настройка времени** - минуты, часы или дни
- **Автоматическая проверка** - по расписанию
- **Детальная статистика** - разбивка по мирам и игрокам
- **Защита онлайн игроков** - не трогает активных игроков
- **Подробные логи** - всё видно в консоли

## Требования

- **Сервер**: Purpur/Paper 1.18.2+
- **Java**: 17 или выше
- **Зависимости**:
  - WorldGuard 7.0+
  - ProtectionStones

## Установка

1. Скачайте последнюю версию из [Releases](../../releases)
2. Поместите `autounclaim-1.0.1.jar` в папку `plugins/`
3. Убедитесь что WorldGuard и ProtectionStones установлены
4. Запустите сервер
5. Настройте `plugins/AutoUnclaim/config.yml`

## Обновление с 1.0.0

Просто замените jar файл - конфигурация полностью совместима!

```bash
# Остановите сервер
./stop.sh

# Замените jar файл
rm plugins/autounclaim-1.0.0.jar
cp autounclaim-1.0.1.jar plugins/

# Запустите сервер
./start.sh
```

## Настройка

```yaml
# Период неактивности
inactive-time:
  value: 90          # Число
  unit: days         # Единица: minutes / hours / days

# Автоматический запуск
auto-run:
  enabled: true      # Включить автопроверку
  interval-minutes: 60  # Проверять каждые 60 минут
```

### Примеры конфигураций

**Для тестирования (проверка каждые 5 минут):**
```yaml
inactive-time:
  value: 1
  unit: minutes

auto-run:
  enabled: true
  interval-minutes: 5
```

**Для маленького сервера:**
```yaml
inactive-time:
  value: 30
  unit: days

auto-run:
  enabled: true
  interval-minutes: 60  # Каждый час
```

**Для большого сервера:**
```yaml
inactive-time:
  value: 90
  unit: days

auto-run:
  enabled: true
  interval-minutes: 1440  # Раз в сутки (24 часа)
```

**Только ручное управление (без автоматики):**
```yaml
inactive-time:
  value: 90
  unit: days

auto-run:
  enabled: false     # Автопроверка отключена
```

## Команды

| Команда | Описание |
|---------|----------|
| `/autounclaim` | Показать справку |
| `/autounclaim run` | Запустить проверку и удалить регионы |
| `/autounclaim run --dry` | 🆕 Предпросмотр (dry-run, без удаления) |
| `/autounclaim reload` | Перезагрузить конфиг |

**Алиасы:** `/auc`, `/unclaim`

**Краткие флаги:** `--dry` или `-d`

## Права доступа

```yaml
autounclaim.use:
  default: op  # Доступ к командам
  
autounclaim.admin:
  default: op  # Полный доступ к плагину
```

## Как это работает

### Автоматический режим
1. Плагин запускается по расписанию (если `auto-run.enabled: true`)
2. Проверяет всех оффлайн игроков
3. Находит тех, кто не заходил дольше указанного времени
4. Удаляет их регионы ProtectionStones (с префиксом `ps`)
5. Логирует результаты в консоль

### Ручной режим
1. Админ вводит команду `/autounclaim run`
2. Плагин выполняет проверку один раз
3. Возвращается в спящий режим

### Dry-run режим 🆕
1. Админ вводит команду `/autounclaim run --dry`
2. Плагин анализирует, что **БУДЕТ** удалено
3. Показывает детальный отчёт **БЕЗ реального удаления**
4. Позволяет принять информированное решение

**Важно:**
- Онлайн игроки всегда защищены от удаления!
- Auto-run всегда использует реальный режим (не dry-run)
- Dry-run доступен только при ручном запуске

## Примеры использования

### Пример 1: Первый запуск (предпросмотр)

```bash
# Проверяем что будет удалено
/autounclaim run --dry
```

**Вывод в игре:**
```
[AutoUnclaim] DRY-RUN REPORT
-------------------------------------
⚠ Regions that WOULD BE removed: 15
⚠ Players that WOULD BE affected: 3

Breakdown by world:
  • world: 8 region(s)
  • world_nether: 5 region(s)
  • world_the_end: 2 region(s)

Affected players:
  InactivePlayer1, InactivePlayer2, InactivePlayer3

Region details (sample):
  • ps123abc (world, owner: InactivePlayer1)
  • ps456def (world, owner: InactivePlayer2)
  ... and 13 more regions

⚠ This was a DRY-RUN. No regions were actually removed.
To actually remove regions, run: /autounclaim run
-------------------------------------
```

### Пример 2: Реальное удаление

```bash
# Убедились что всё правильно - удаляем
/autounclaim run
```

**Вывод в игре:**
```
[AutoUnclaim] Check completed!
-------------------------------------
Removed regions: 15
Affected players: 3

Breakdown by world:
  • world: 8 region(s)
  • world_nether: 5 region(s)
  • world_the_end: 2 region(s)
-------------------------------------
```

### Пример 3: Тестирование конфигурации

```bash
# Хотим проверить разные настройки времени

# Вариант 1: 60 дней
inactive-time.value: 60
/autounclaim reload
/autounclaim run --dry
> Result: 450 regions

# Вариант 2: 90 дней
inactive-time.value: 90
/autounclaim reload
/autounclaim run --dry
> Result: 280 regions

# Вариант 3: 120 дней
inactive-time.value: 120
/autounclaim reload
/autounclaim run --dry
> Result: 150 regions

# Выбираем оптимальный вариант и запускаем реально
/autounclaim run
```

## Примеры логов

**При запуске сервера:**
```
[AutoUnclaim] Plugin loaded! Inactive period: 90 дней
[AutoUnclaim] Auto-run enabled! Interval: 60 minute(s)
[AutoUnclaim] Auto-check started (every 60 minute(s))
```

**При dry-run проверке:**
```
[AutoUnclaim] Found 3 inactive player(s) (not logged in > 90 дней)
[AutoUnclaim] Affected players: Player1, Player2, Player3
```

**При реальном удалении:**
```
[AutoUnclaim] Found 3 inactive player(s) (not logged in > 90 дней)
[AutoUnclaim] Removed region: ps123abc (owner: Player1, world: world)
[AutoUnclaim] Removed region: ps456def (owner: Player2, world: world)
[AutoUnclaim] Removed region: ps789ghi (owner: Player1, world: world_nether)
[AutoUnclaim] Affected players: Player1, Player2, Player3
```

**При автопроверке:**
```
[AutoUnclaim] Auto-run: Starting inactive player check...
[AutoUnclaim] Auto-run completed: removed 5 region(s), affected 2 player(s)
[AutoUnclaim]   - world: 3 region(s)
[AutoUnclaim]   - world_nether: 2 region(s)
```

## Сборка из исходников

### Требования
- JDK 17+
- Gradle 7.0+

### Команды

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/ps-unclaim.git
cd ps-unclaim

# Собрать плагин
./gradlew shadowJar

# Готовый файл
build/libs/autounclaim-1.0.1.jar
```

## Структура проекта

```
ps-unclaim/
├── src/main/
│   ├── kotlin/org/odeindev/autounclaim/
│   │   ├── AutoUnclaim.kt       # Главный класс
│   │   ├── ExecutionMode.kt     # 🆕 Режимы выполнения
│   │   ├── RegionPruner.kt      # Логика удаления
│   │   └── UnclaimCommand.kt    # Команды
│   └── resources/
│       ├── plugin.yml
│       └── config.yml
└── build.gradle.kts
```

## Частые вопросы

**В: Удалит ли регионы у игроков онлайн?**  
О: Нет. Плагин автоматически исключает онлайн игроков.

**В: Можно протестировать без ожидания 90 дней?**  
О: Да! Установите `inactive-time: value: 1, unit: minutes` в конфиге.

**В: Как отключить автопроверку?**  
О: Установите `auto-run.enabled: false` в конфиге.

**В: Работает ли с другими плагинами регионов?**  
О: Только с ProtectionStones (регионы с префиксом `ps`).

**В: Что такое dry-run и зачем он нужен?** 🆕  
О: Dry-run показывает что **БУДЕТ** удалено, но **НЕ удаляет** на самом деле. Это позволяет проверить результат перед реальным удалением.

**В: Использует ли автоматическая проверка dry-run режим?** 🆕  
О: Нет. Auto-run всегда удаляет регионы реально. Dry-run доступен только при ручном запуске команды `/autounclaim run --dry`.

**В: Можно ли запустить dry-run несколько раз подряд?** 🆕  
О: Да! Dry-run можно запускать сколько угодно раз - он ничего не меняет и всегда показывает актуальное состояние.

## Решение проблем

**Плагин не загружается:**
- Проверьте что WorldGuard и ProtectionStones установлены
- Убедитесь что Java 17 или выше
- Посмотрите логи на ошибки

**Регионы не удаляются:**
- Проверьте что ID региона начинается с `ps`
- Убедитесь что игрок действительно оффлайн
- Проверьте что прошёл период неактивности
- Используйте `/autounclaim run --dry` для диагностики

**Dry-run показывает неожиданные результаты:** 🆕
- Проверьте настройку `inactive-time` в конфиге
- Убедитесь что даты lastPlayed корректны
- Проверьте не объединяли ли вы сервера (даты могут сброситься)

**OutOfMemoryError:**
- Увеличьте RAM сервера: `-Xmx4G` вместо `-Xmx2G`

## Технологии

- **Язык**: Kotlin 1.8.0
- **Сборка**: Gradle (Kotlin DSL)
- **API**: Paper API, WorldGuard API
- **Плагин**: ShadowJar

## Changelog

### v1.0.1 (16.02.2026) 🆕
- **Добавлен dry-run режим** (`/autounclaim run --dry`)
- Детальная статистика по мирам
- Расширенный отчёт с примерами регионов
- Улучшенное логирование в консоль
- Рефакторинг кода для лучшей читаемости
- Обновлена документация

### v1.0.0 (15.02.2026)
- Первый релиз
- Автоматические проверки по расписанию
- Защита онлайн игроков
- Гибкие единицы времени (минуты/часы/дни)
- Ручной запуск команд
- Подробное логирование

## Лицензия

MIT License - см. [LICENSE](LICENSE)

## Автор

**odeindev** - [GitHub](https://github.com/odeindev)

## Поддержка

Если вы нашли баг или есть вопросы:
- Откройте [Issue](../../issues)
- Проверьте существующие issues
- Посмотрите раздел [Частые вопросы](#частые-вопросы)

**Сделано для сообщества Minecraft** ❤️

**v1.0.1** | [Changelog](CHANGELOG.md)
